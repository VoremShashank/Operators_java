public class ternary {
    public static void main(String[] args) {
        int marks = 31;
        String results = (marks>=33) ? "pass" : "fail";
        System.out.println(results);
        
    }
    
}
/* ternary operators are used instead of if else 
 * it is writtern in a line 
 */
